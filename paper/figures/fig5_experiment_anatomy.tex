% Figure: anatomy of the experiment (conceptual opener for Sec. 5).
% Two counterfactual lanes branch from one frozen start. Scores are sealed
% once, before any objective is run; each lane then realizes its own damage
% pattern, and the same sealed rankings enter prediction and protection.
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  x=1cm,y=1cm,
  font=\scriptsize,
  >=Latex,
  box/.style={draw=gray!55,rounded corners=1.2mm,fill=white,
              align=center,inner sep=1.6mm},
  lane/.style={draw,rounded corners=1.5mm,fill=white,
               align=center,inner sep=1.2mm},
  chip/.style={draw=gray!60,rounded corners=0.7mm,fill=white,
               align=center,inner xsep=0.8mm,inner ysep=0.55mm,font=\tiny},
  hdr/.style={font=\footnotesize\bfseries,align=left},
  stagebadge/.style={circle,draw=gray!65,fill=gray!8,minimum size=4.4mm,
                    inner sep=0pt,font=\scriptsize\bfseries},
  note/.style={align=center,text=gray!60!black,font=\tiny},
  arr/.style={->,thick,gray!60!black,shorten >=0.5mm},
  bus/.style={->,densely dashed,thick,gray!60!black,shorten >=0.5mm}
]

% Fix the native aspect ratio near the ACM two-column text block.  The
% resize is therefore slight and does not compensate for a very wide canvas.
\path[use as bounding box] (-0.05,-0.60) rectangle (17.15,6.65);

% ---------- stage headers: separate number nodes avoid fragile \textcircled ----------
\node[stagebadge] at (0.28,6.28) {1};
\node[hdr,anchor=west,text width=2.55cm] at (0.58,6.28)
  {Score once\\and seal};

\node[stagebadge] at (3.72,6.28) {2};
\node[hdr,anchor=west,text width=4.20cm] at (4.02,6.28)
  {Declare the channel;\\run its objectives};

\node[stagebadge] at (8.92,6.28) {3};
\node[hdr,anchor=west,text width=3.20cm] at (9.22,6.28)
  {Realize lane-specific\\damage};

\node[stagebadge] at (13.12,6.28) {4};
\node[hdr,anchor=west,text width=3.45cm] at (13.42,6.28)
  {Evaluate prediction\\and protection};

% ---------- (1) frozen start and sealed scores ----------
\node[box,text width=2.62cm,minimum height=1.72cm] (start) at (1.68,4.42)
  {\textbf{One frozen start}\\
   SFT'd model $\theta_0$\\
   forget set $\mathcal D_f$\\
   candidates $x_1{:}x_n$};

\node[box,text width=2.62cm,minimum height=2.02cm,draw=black,thick]
  (scores) at (1.68,1.77)
  {\textbf{Sealed pre-scores}\\[0.35mm]
   {\color{orange!75!black}gradient-energy rank}\\
   {\color{teal!60!black}proximity rank}\\
   {\color{gray}baselines}\\[0.35mm]
   {\tiny computed once; never revised}};

\draw[arr] (start.south) -- (scores.north);

% ---------- (2) channel lanes ----------
\node[lane,draw=orange!70!black,fill=orange!5,text width=4.18cm,
      minimum height=2.18cm] (laneO) at (6.15,4.42) {};
\node[font=\scriptsize\bfseries] at ([yshift=7.6mm]laneO.center)
  {Output-channel lane};
\node[note,text width=4.02cm] at ([yshift=3.1mm]laneO.center)
  {loss reads token likelihoods $\Rightarrow$ high-gradient victims};
\node[chip] at ([xshift=-1.53cm,yshift=-3.0mm]laneO.center) {GradDiff};
\node[chip] at ([xshift=-0.52cm,yshift=-3.0mm]laneO.center) {NPO};
\node[chip] at ([xshift= 0.48cm,yshift=-3.0mm]laneO.center) {SimNPO};
\node[chip] at ([xshift= 1.46cm,yshift=-3.0mm]laneO.center) {GRU};
\node[note] at ([yshift=-8.2mm]laneO.center)
  {one objective per world; the same $\theta_0$};

\node[lane,draw=teal!60!black,fill=teal!6,text width=4.18cm,
      minimum height=2.18cm] (laneR) at (6.15,1.77) {};
\node[font=\scriptsize\bfseries] at ([yshift=7.6mm]laneR.center)
  {Representation-channel lane};
\node[note,text width=4.02cm] at ([yshift=3.1mm]laneR.center)
  {loss reads hidden states $\Rightarrow$ forget-adjacent victims};
\node[chip] at ([xshift=-1.47cm,yshift=-3.0mm]laneR.center) {RMU};
\node[chip] at ([xshift=-0.14cm,yshift=-3.0mm]laneR.center)
  {RepNoise$^{\mathrm a}$};
\node[chip] at ([xshift= 1.43cm,yshift=-3.0mm]laneR.center)
  {RR/CB$^{\mathrm a}$};
\node[note] at ([yshift=-8.2mm]laneR.center)
  {$^{\mathrm a}$adapted implementations (Appendix)};

% A short fork uses the otherwise empty gutter; no arrow crosses a box.
\coordinate (fork) at ([xshift=3.3mm]start.east);
\draw[arr] (start.east) -- (fork) -- (laneO.west);
\draw[arr] (fork) |- (laneR.west);
\fill[gray!60!black] (fork) circle (0.55pt);

\node[note,text width=4.20cm] at (6.15,0.38)
  {GA and IdkDPO are appendix stress controls, not lane members};

% ---------- (3) lane-specific realized damage ----------
\node[box,text width=3.10cm,minimum height=1.74cm] (dO) at (10.92,4.42) {};
\node[align=center] at ([yshift=4.0mm]dO.center)
  {\textbf{Output damage}\\$d^{\mathcal U_{\mathrm{out}}}(x)$};

\node[box,text width=3.10cm,minimum height=1.74cm] (dR) at (10.92,1.77) {};
\node[align=center] at ([yshift=4.0mm]dR.center)
  {\textbf{Representation damage}\\$d^{\mathcal U_{\mathrm{rep}}}(x)$};

% Six fixed candidate slots; filled positions are lane-specific victims.
\newcommand{\victimstrip}[9]{%
  \foreach [count=\i] \flag in {#3,#4,#5,#6,#7,#8}{%
    \ifnum\flag=1
      \fill[#9] ({#1+(\i-1)*0.34},{#2})
        rectangle ({#1+(\i-1)*0.34+0.28},{#2+0.30});
    \else
      \draw[gray!45] ({#1+(\i-1)*0.34},{#2})
        rectangle ({#1+(\i-1)*0.34+0.28},{#2+0.30});
    \fi}}
\victimstrip{9.94}{3.83}{1}{1}{0}{0}{1}{0}{orange!75!black}
\victimstrip{9.94}{1.18}{0}{0}{1}{1}{0}{1}{teal!60!black}

\draw[arr] (laneO.east) -- (dO.west);
\draw[arr] (laneR.east) -- (dR.west);

\node[note,text width=3.35cm] at (10.92,0.38)
  {same candidates, different victims; the ideal list is empty};

% ---------- (4) one roomy evaluation panel with two internal tests ----------
\node[box,text width=3.20cm,minimum height=4.80cm] (eval) at (15.18,3.10) {};

\node[align=center,text width=2.88cm] (pred) at ([yshift=12.5mm]eval.center)
  {\textbf{Prediction}\\
   {\tiny Tab.~\ref{tab:channel-matrix}}\\[0.35mm]
   $\rho(\text{sealed score},d^{\mathcal U})$\\
   {\tiny per objective in both lanes}\\[0.35mm]
   {\tiny channel interaction $\Delta$}};

\draw[gray!35] ([xshift=2.0mm]eval.west) -- ([xshift=-2.0mm]eval.east);

\node[align=center,text width=2.88cm] (protect) at ([yshift=-12.0mm]eval.center)
  {\textbf{Protection}\\
   {\tiny Tab.~\ref{tab:crossed}}\\[0.35mm]
   Top-$K_p$ by one sealed score;\\
   repair, then re-measure\\
   audit CVaR\\[0.35mm]
   {\tiny matched forgetting within each world}};

% Separate input ports keep both arrows horizontal and readable.
\draw[arr] (dO.east) -- (eval.west |- dO.east);
\draw[arr] (dR.east) -- (eval.west |- dR.east);

% The sealed-score path gets its own bottom rail instead of a long curve.
\draw[bus] (scores.south) -- ++(0,-0.63) -| (eval.south);
\node[note,fill=white,inner sep=0.6mm] at (8.45,-0.18)
  {the same sealed rankings enter every comparison and every protect pool};

\end{tikzpicture}%
}
