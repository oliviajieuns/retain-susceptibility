# Fill from development-only calibration and commit this file BEFORE launching
# the audit phase. run_campaign.py refuses to audit while status is not frozen.
# Procedure (identical to the 7B WMDP campaign):
#   1. drain fidelity + calibration on wave_wmdp14b,
#   2. run experiments/channel_matrix/select_objective_freeze.py (or the
#      documented selector) over the development results,
#   3. paste the per-objective lr/steps here, set status: frozen and
#      frozen_before_audit: true with a real freeze_id + UTC timestamp,
#   4. commit, THEN enqueue the audit phase.
freeze_id: PENDING-WMDP14B-DEVELOPMENT-CALIBRATION
status: draft
frozen_before_audit: false
frozen_at_utc: null
source_campaign: kdd27-channel-matrix-wmdp14b-v1
unresolved: [qwen25_14b/all-objectives]
selection_rule: >-
  Core objectives must satisfy forget_recall <= 0.10 and the fixed mean/CVaR
  utility bounds on both development requests. Stress controls prefer settings
  reaching the forget criterion; if none do, they minimize worst development
  recall. All ties use retained damage, steps, then learning rate; no predictor
  score or correlation is read.
models:
  qwen25_14b:
    ga: {lr: null, steps: null}
    graddiff: {lr: null, steps: null, forget_weight: 1.0, retain_weight: 1.0}
    npo: {lr: null, steps: null, beta: 0.1, forget_weight: 1.0, retain_weight: 1.0}
    simnpo: {lr: null, steps: null, beta: 4.5, forget_weight: 0.125, retain_weight: 1.0}
    gru: {lr: null, steps: null, forget_weight: 1.0, retain_weight: 1.0}
    rmu: {lr: null, steps: null, rmu_alpha: null, rmu_c: null}
    repnoise: {lr: null, steps: null, rmu_alpha: null, rmu_c: null}
    circuit_breakers: {lr: null, steps: null, rmu_alpha: null}
